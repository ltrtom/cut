extends index

block header


block content
    form(method='POST', action='/login')#form-login
            input(type='text', name='login', placeholder='login', value=lastLogin).form-control.text-center
            input(type='password', name='pass', placeholder='password').form-control.text-center
            input(type='submit', value='login').btn.btn-primary   
            if message
              .alert.alert-danger.text-center
                =message
    // div.modal-backdrop.fade.in